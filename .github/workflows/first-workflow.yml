name: First Workflow

on: push  

jobs:
  say_hello:
    runs-on: ubuntu-latest 
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Print a message
        run: echo "Workflow checking!"

